#!/usr/bin/env sh
# Pre-push: full typed ESLint + TypeScript build check.
# This runs the slow but thorough strictTypeChecked analysis before pushing.
echo "Running typed ESLint (strictTypeChecked)..."
cd client && npx eslint src --config eslint.config.js
